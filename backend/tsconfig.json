{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2022",                      // Modern JS output
    "module": "nodenext",                    // Native ES module support in Node.js
    "moduleResolution": "nodenext",              // Classic Node.js module resolution
    "lib": ["es2022"],                       // Built-in lib for modern JS
    "esModuleInterop": true,                 // Allows default import of CommonJS modules
    "forceConsistentCasingInFileNames": true,// Prevents casing mismatches in imports
    "strict": true,                          // All strict type-checking options
    "skipLibCheck": true,                    // Skips type checks for  external .d.ts files

    /* Paths */
      "rootDir": "./src",                      // Your main source directory
      "outDir": "./dist",                      // Output build directory
      "typeRoots": ["./node_modules/@types", "./src/types"], // Custom types

      /* Output */
      "removeComments": true,                  // Clean compiled files
      "sourceMap": true                        // Enable source maps for debugging
  },

  "include": ["src/**/*"],                   // All source files
  "exclude": ["node_modules", "dist"]        // Exclude build artifacts
}
